---
description: semantic-release on main (stable) and rc/* (prerelease); Conventional Commits; rc train before main.
alwaysApply: true
---

# Releases (semantic-release)

- Pushes to **`main`** or **`rc/*`** run `.github/workflows/release.yml` → **semantic-release** (`.releaserc.json`).
- **`main`**: **stable** releases `x.y.z`.
- **`rc/*`**: **prereleases** `x.y.z-rc.n` (GitHub “Pre-release”) while integrating before merging into `main`.
- **Semver** is derived from **Conventional Commits** since the last git tag: `feat` → minor, `fix` / `perf` → patch, `BREAKING CHANGE` or `feat!` → major (see [Angular preset](https://github.com/conventional-changelog/conventional-changelog/tree/master/packages/conventional-changelog-angular)).
- **`ci`**, **`chore`**, **`docs`**, **`refactor`**, **`test`** commits usually **do not** produce a new version unless configured otherwise—use **`fix:`** or **`feat:`** when a release is intended.
- **CHANGELOG.md** and **package.json** version are committed by the release job with **`[skip ci]`** to avoid loops.
- **PR titles** into **`main`** / **`rc/*`**: enforced by `semantic-pr-title.yml`. For **`feat/*` → `rc/*`**, **squash** + conventional title is typical. For **`rc/*` → `main`**, prefer a **merge commit** (not squash) so **semantic-release** sees each `feat`/`fix` on `main`.
- When a feature should **ship in the changelog**, use **`feat:`** / **`fix:`** (or merge commit subjects on `main` that stay conventional).
